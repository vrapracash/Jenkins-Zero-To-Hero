pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  stage('Deploy') {
      steps {
        sh 'java -version'
      }
    }
  stage('Results') {
      steps {
        sh 'docker run Hello-World'
      }
    }
  }
}
